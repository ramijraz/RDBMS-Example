1.  CREATE TABLE DEPT (
	DEPT_ID NUMBER (7),
	DEPT_NAME VARCHAR2 (20),
	PRIMARY KEY (DEPT_ID)
	);
	 
2.  INSERT INTO DEPT SELECT DEPARTMENT_ID,DEPARTMENT_NAME FROM HR.DEPARTMENTS;
    INSERT INTO DEPT VALUES(10,'ACCOUNTS');
	INSERT INTO DEPT VALUES(NULL,'ACCOUNTS');
--  insert into dept values ('A1', 'Accounts');
	UPDATE DEPT SET DEPT_ID = 30 WHERE DEPT_NAME='ACCOUNTS';
	
3.	CREATE TABLE EMP(
	ID NUMBER(7),
	LAST_NAME VARCHAR2(25) NOT NULL,
	FIRST_NAME VARCHAR2(25),
	DEPT_ID NUMBER(7),
	FOREIGN KEY(DEPT_ID) REFERENCES DEPT(DEPT_ID)
	);
	DESCRIBE EMP;
	INSERT ALL 
	INTO EMP(101,'SAM','SUNDAR',10)
	INTO EMP(102,'RAM','KRISNA',20)
	INTO EMP(103,'GOPI',NULL,40)
--	INTO EMP(104,NULL,'RAM',20)
	SELECT 1 FROM DUAL;
	
	